import { Cardano } from '@cardano-sdk/core';
import { ChangeAddressResolver, Selection } from '../index.js';
import { GroupedAddress } from '@cardano-sdk/key-management';
export declare type GetAddresses = () => Promise<GroupedAddress[]>;
export declare class StaticChangeAddressResolver implements ChangeAddressResolver {
    #private;
    constructor(getAddresses: GetAddresses);
    resolve(selection: Selection): Promise<Array<Cardano.TxOut>>;
}
//# sourceMappingURL=StaticChangeAddressResolver.d.ts.map